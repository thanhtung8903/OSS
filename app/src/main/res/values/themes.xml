<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.OSS" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.OSS" parent="Base.Theme.OSS" />

    <!-- Thêm style custom cho disabled field -->
    <style name="CustomTextInputLayout" parent="Widget.Material3.TextInputLayout.OutlinedBox">
        <item name="helperTextTextAppearance">@style/HelperTextAppearance</item>
    </style>

    <style name="HelperTextAppearance" parent="TextAppearance.Material3.BodySmall">
        <item name="android:textColor">@color/on_surface_variant</item>
    </style>

    <!-- AppBar Overlay styles -->
    <style name="Theme.OSS.AppBarOverlay" parent="ThemeOverlay.Material3.Dark.ActionBar" />

    <style name="Theme.OSS.PopupOverlay" parent="ThemeOverlay.Material3.Light" />
</resources>